/**[@test({ "title": "TruJS.cli.operations._Run: " })]*/
function testRunOperation(arrange, act, assert, callback, promise, module) {
  var runOperation;

  arrange(function () {

    runOperation = module(["TruJS.cli.operations._Run", []]);

  });

  act(function () {

  });

  assert(function (test) {

  });
}
