/**[@test({ "title": "TruJS.cli.operations._Serve: " })]*/
function testServeOperation(arrange, act, assert, callback, promise, module) {
  var serveOperation;

  arrange(function () {

    serveOperation = module(["TruJS.cli.operations._Serve", []]);

  });

  act(function () {

  });

  assert(function (test) {

  });
}
