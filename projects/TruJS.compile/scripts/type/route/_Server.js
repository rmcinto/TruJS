/**
* This factory produces a worker function that implements the $$routes$$ and
* $$apps$$ module entries with the express, http, and https modules using the
* configuration object passed to the worker function.
* @factory
*/
function _Server(promise, $$apps$$, $$routes$$) {

  /**
  * @worker
  * @param {object} config The configuration object
  */
  return function Server(config) {



  };
}